<% if notice %>
  <p id="notice"><%= notice %></p>
<% end %>

<h1>Your Beer Catalog</h1>

<% @brews.each do |brew| %>
  <div class="entry">
    <%= image_tag(brew.image_url) %>
    <h3><%= brew.title %></h3>
    <%= sanitize(brew.description) %>
    <div class="price_line">
      <span class="price"><%= number_to_currency(product.price) %></span>
    </div>
  </div>
<% end %>
