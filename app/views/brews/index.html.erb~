<h1>Listing brews</h1>

<table>


<tbody>
<th colspan="2">Beer</th>
<th>Origin</th>
<th>Website</th>
<th>Price</th>
    <% @brews.each do |brew| %>
     <%= content_tag :tr, class: cycle('list_line_odd', 'list_line_even') do %>
  <td>
    <%= image_tag(brew.image_url, class: 'list_image') %>
  </td>
<td class='list_description'>
  <dl>
    <dt><%= brew.title %></dt>
    <dd>
      <%= truncate strip_tags(brew.description), length: 150 %>
    </dd>
  </dl>
</td>
        
        <td><%= brew.origin %></td>
        <td><a href="<%=brew.website %>"><%= brew.website %></a></td>
        <td><%= brew.price %></td>
        <td class='list_actions'>
  <%= link_to 'Show', product %><br/>
  <%= link_to 'Edit', edit_product_path(product) %><br/>
  <%= link_to 'Destroy', product, method: :delete, 
      data: { confirm: 'Are you sure?' } %>
</td>
      <% end %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Brew', new_brew_path %>
